# meridian

Rust workspace for Meridian.

## Development

Enter the nightly Rust development environment with Nushell:

```nu
nix develop
```

The flake starts Nushell for interactive shells. For an ad-hoc command without
starting an interactive shell, use `nix develop -c <command>`.
