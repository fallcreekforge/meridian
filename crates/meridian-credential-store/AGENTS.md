# meridian-credential-store

- Credentials stay inside customer-controlled infrastructure.
- Never expose secrets through logs, `Debug`, display text, or errors.
- Add one concrete backend only when its use is implemented and tested.
