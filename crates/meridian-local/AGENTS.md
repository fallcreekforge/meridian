# meridian-local

- Keep reusable customer-side orchestration in `SyncEngine`.
- Depend on credential and platform abstractions, not production implementations.
- Produce protocol types without adding cloud transport or CLI concerns.
