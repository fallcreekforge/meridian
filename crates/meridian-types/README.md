# meridian-types

Contains identifiers shared by Meridian's public crates.

`StudioId` and `PlatformGameId` are distinct opaque string types, preventing accidental mixups
between identifier domains. This crate contains no platform, credential, storage, CLI, or cloud
behavior.
