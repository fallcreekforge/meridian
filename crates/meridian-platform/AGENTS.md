# meridian-platform

- Keep the contract platform-neutral and limited to capabilities used by local orchestration.
- Add platforms explicitly; do not introduce arbitrary endpoint or value bags.
- Keep credentials local and pass them only to the platform adapter that needs them.
