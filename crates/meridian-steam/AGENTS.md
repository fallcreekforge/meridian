# meridian-steam

- Keep capabilities typed and narrow; do not expose a generic endpoint API.
- Do not depend on local orchestration, the sync protocol, the CLI, or cloud concepts.
- Never log authentication material or include it in errors.
